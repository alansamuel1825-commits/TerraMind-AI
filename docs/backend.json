
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User account information and permissions.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "role": { "type": "string", "enum": ["director", "analyst"] },
        "name": { "type": "string" }
      }
    },
    "AgriAnalysis": {
      "title": "Agriculture Analysis",
      "description": "Captured crop health analysis data.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "plantName": { "type": "string" },
        "isHealthy": { "type": "boolean" },
        "confidence": { "type": "number" },
        "imageUrl": { "type": "string" },
        "timestamp": { "type": "number" }
      }
    },
    "EnergySimulation": {
      "title": "Energy Grid Simulation",
      "description": "Saved building energy load profiles.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "buildingType": { "type": "string" },
        "efficiencyRating": { "type": "number" },
        "timestamp": { "type": "number" }
      }
    },
    "WasteAnalysis": {
      "title": "Waste Classification",
      "description": "AI-powered waste sorting results.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "category": { "type": "string" },
        "isRecyclable": { "type": "boolean" },
        "confidence": { "type": "number" },
        "timestamp": { "type": "number" }
      }
    },
    "CitySimulation": {
      "title": "Smart City Simulation",
      "description": "Digital twin scenario parameters.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "sustainabilityScore": { "type": "number" },
        "timestamp": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "Stores core user metadata."
    },
    "/agri_analyses/{analysisId}": {
      "schema": "#/backend/entities/AgriAnalysis",
      "description": "Stores history of agricultural health scans."
    },
    "/energy_simulations/{simId}": {
      "schema": "#/backend/entities/EnergySimulation",
      "description": "Stores building load simulations."
    },
    "/waste_analyses/{analysisId}": {
      "schema": "#/backend/entities/WasteAnalysis",
      "description": "Stores waste classification history."
    },
    "/city_simulations/{simId}": {
      "schema": "#/backend/entities/CitySimulation",
      "description": "Stores smart city scenario data."
    },
    "/environmental_reports/{reportId}": {
      "schema": "object",
      "description": "Stores environmental indicator reports."
    },
    "/chat_threads/{threadId}": {
      "schema": "object",
      "description": "Stores AI assistant conversation threads."
    }
  }
}
